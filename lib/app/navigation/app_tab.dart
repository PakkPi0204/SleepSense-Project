/// The four bottom-navigation destinations, in display order. The enum index is
/// also the IndexedStack index in SleepSenseShell, so order matters here.
enum AppTab { home, patterns, sleep, settings }
